类中的变量分四种：成员变量(包括类变量和实例变量)、局部变量、引用变量。
    在jdk1.8之前的jvm中内存位置：
        类变量（类中static修饰的变量）：方法区
        实例变量（普通变量，包括实例对象和基础数据类型）：堆heap
        局部变量（方法中声明的变量）：栈(虚拟机栈)
        引用变量（个人理解是声明变量时，=号之前的变量，例如String str;）：栈(虚拟机栈)
